import React from 'react';

const styles = {
    height: 48,
    backgroundColor: 'rgb(39, 102, 150)',
    zIndex: 1100,
};

const Header = () => (
    <div style={styles}>
    </div>
);

export default Header;