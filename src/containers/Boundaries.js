import { connect } from 'react-redux';
import Boundaries from '../components/edit/Boundaries';

const mapStateToProps = (state) => ({
});

export default connect(
  mapStateToProps,
  {
  }
)(Boundaries);
