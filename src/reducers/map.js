const defaultState = {
    latitude: 8.4,
    longitude: -11.8,
    zoom: 8 ,
};


const map = (state = defaultState, action) => {

    switch (action.type) {

        default:
            return state;

    }
};

export default map;